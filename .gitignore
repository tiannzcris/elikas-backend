*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
# Desktop installer -- 123MB, exceeds GitHub's 100MB hard file limit and
# doesn't belong in source control regardless. Upload directly to the
# hosting server (WinSCP) instead of committing it.
/public/downloads/*.exe
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
